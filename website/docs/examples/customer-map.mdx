---
title: Where our customers are
description: A "used in 68 countries" map — choropleth plus city bubbles, with the headline painted into the canvas so it exports as a finished graphic.
keywords:
  - map showing customers
  - customer locations map
  - users around the world map
  - where our customers are
---

import GlobeDemo from "@site/src/components/GlobeDemo";

# Where our customers are

:::note Example data
All customer counts and locations on this page are fictional demonstration
data. They are not CanvasGlobe usage claims.
:::

The single most requested marketing map: **proof that people outside your own postcode pay you.**
Two layers do the work — countries shaded by volume so the eye reads the spread instantly, and city
bubbles sized by count so the density is honest.

<GlobeDemo
  options={{
    mode: "map",
    preset: "political",
    autoRotate: false,
    tooltip: true,
    markerStyle: "bubble",
    markerScale: 0.9,
    onCountryHover: () => {},
    countryColors: {
      IN: "#4c1d95", US: "#5b21b6", GB: "#6d28d9", DE: "#7c3aed", CA: "#8b5cf6",
      BR: "#8b5cf6", AU: "#a78bfa", JP: "#a78bfa", SG: "#a78bfa", FR: "#a78bfa",
      NL: "#c4b5fd", AE: "#c4b5fd", ZA: "#ddd6fe", KE: "#ddd6fe", MX: "#c4b5fd",
    },
    legend: { title: "Customers", scale: { domain: [0, 420], range: ["#ede9fe", "#4c1d95"] } },
    title: { text: "Used by teams in 68 countries", position: "top-left", size: 26 },
    markers: [
      { city: "Ahmedabad", lat: 23.03, lon: 72.58, count: 214 },
      { city: "Bengaluru", lat: 12.97, lon: 77.59, count: 186 },
      { city: "London", lat: 51.5, lon: -0.12, count: 164 },
      { city: "New York", lat: 40.71, lon: -74.01, count: 152 },
      { city: "San Francisco", lat: 37.77, lon: -122.42, count: 141 },
      { city: "Berlin", lat: 52.52, lon: 13.4, count: 98 },
      { city: "Toronto", lat: 43.65, lon: -79.38, count: 84 },
      { city: "São Paulo", lat: -23.55, lon: -46.63, count: 76 },
      { city: "Singapore", lat: 1.35, lon: 103.82, count: 71 },
      { city: "Sydney", lat: -33.87, lon: 151.21, count: 63 },
      { city: "Tokyo", lat: 35.68, lon: 139.69, count: 58 },
      { city: "Dubai", lat: 25.2, lon: 55.27, count: 47 },
      { city: "Mexico City", lat: 19.43, lon: -99.13, count: 42 },
      { city: "Nairobi", lat: -1.29, lon: 36.82, count: 31 },
      { city: "Lagos", lat: 6.52, lon: 3.38, count: 28 },
    ],
  }}
  controls={[
    { type: "select", label: "Look", option: "preset", values: ["political", "mono", "midnight", "atlas", "blueprint"] },
    { label: "City bubbles", option: "markerStyle", value: "bubble" },
    { label: "Countries only", option: "markerStyle", value: "dot" },
    { type: "select", label: "Projection", option: "projection", values: ["equirectangular", "naturalEarth", "mercator"] },
  ]}
  caption="Hover a country or a city. The headline is drawn on the canvas, so it survives an export."
/>

## The code

```jsx
import { createGlobe, colorScale } from "canvas-globe";

// Whatever your analytics already returns.
const byCountry = { IN: 412, US: 388, GB: 214, DE: 168, CA: 96, BR: 84 /* … */ };
const cities = [
  { city: "Ahmedabad", lat: 23.03, lon: 72.58, count: 214 },
  { city: "London", lat: 51.5, lon: -0.12, count: 164 },
  // …
];

const scale = colorScale([0, 420], ["#ede9fe", "#4c1d95"]);

const globe = createGlobe(canvas, {
  mode: "map",
  preset: "political",
  projection: "naturalEarth",

  countryColors: Object.fromEntries(
    Object.entries(byCountry).map(([iso, n]) => [iso, scale(n)]),
  ),

  markers: cities,
  markerStyle: "bubble",
  markerScale: 0.9,

  legend: { title: "Customers", scale: { domain: [0, 420], range: ["#ede9fe", "#4c1d95"] } },
  title: { text: `Used by teams in ${Object.keys(byCountry).length} countries` },

  tooltip: (m) => `${m.city} — ${m.count} customers`,
  onCountryClick: (shape) => filterTable(shape.iso),
});
```

## Why these choices

**Map, not globe.** A globe hides half the world at any moment. For a *count* — "we are in 68
countries" — hiding half of them undercuts the claim. Use the globe when the motion is the message;
use the map when the coverage is.

**`naturalEarth` over `mercator`.** Mercator inflates Europe and North America and shrinks
everywhere near the equator. If a chunk of your customer base is in India, Brazil, Nigeria or
Indonesia, Mercator makes your business look smaller than it is. Natural Earth is the polite
default. See [Projections](/guides/projections).

**Two layers, one story.** Countries answer *where*, bubbles answer *how many*. Either alone leaves
a question open, and a viewer with an open question scrolls.

**A `title` on the canvas, not in a `<h2>`.** It costs the same to draw and it means
`exportImage()` gives you the finished asset for a deck, a tweet or an email — no design tool in the
loop. See [Social share card generator](/examples/social-card).

## Joining data you already have

Country codes rarely arrive in the shape you want. Keys are matched case-insensitively against ISO
alpha-2, then the numeric id, then the country name — so most joins need no mapping at all:

```js
countryColors: { in: "#4c1d95", "United States": "#5b21b6", 826: "#6d28d9" }
```

If your warehouse speaks alpha-3, map it once:

```js
const alpha2 = { IND: "IN", USA: "US", GBR: "GB" /* … */ };
countryColors: Object.fromEntries(rows.map((r) => [alpha2[r.iso3], scale(r.total)])),
```

For a spreadsheet export with city names and no coordinates, [`fromCSV`](/guides/data) resolves
around 300 bundled cities plus every country, and hands back a `skipped` list so you know exactly
what did not match:

```js
import { fromCSV } from "canvas-globe";

const markers = fromCSV(csvText);          // reads city / country / count columns
if (markers.skipped.length) console.warn("Unresolved rows:", markers.skipped);
globe.setMarkers(markers);
```

## Keeping it honest

A map of customers is a claim, and claims get checked.

- **Do not plot one customer as a bubble the size of a country.** `markerScale` is a multiplier on
  a square-root scale, so a city with 200 customers is already only ~4.5× the radius of one with 10.
  Resist the urge to exaggerate it.
- **Say what a dot is.** "Customers", "signups", "trial accounts" and "waitlist entries" are wildly
  different numbers. Put the word in the legend title.
- **Do not scatter fake dots to fill empty ocean-facing continents.** Anyone who works in sales at
  your company will notice, and they are the people who repost it.

## Where it goes

| Placement | Change |
| --- | --- |
| Above the fold | Drop the legend, keep the title, cap it at ~420 px tall |
| Pricing page | Colour by *plan availability* instead of volume |
| Sales deck | `exportImage({ width: 2400, height: 1260 })` |
| Investor update | Add [`playTimeline`](/guides/overlays) to animate the last four quarters |
| Email | Export a PNG — no email client will run canvas |

## Next

- [Choropleth](/guides/choropleth) — scales, legends and country click handling
- [Live signup social proof](/examples/live-signups) — the same data, as events instead of totals
- [Coverage dashboard](/examples/coverage-dashboard) — the in-product version of this map
